# Fix pass — 19 September 2026

> **What this is.** The record of what was corrected after
> [`AUDIT-2026-09-18.md`](AUDIT-2026-09-18.md), one entry per class of defect, with the
> evidence used to check each correction. The previous audit says what the material claimed;
> this one says what changed and how it was verified. Both are snapshots.
>
> **How the fixes were made.** Seven passes, one per module group — eJPT · eCPPT · eWPT,
> eWPTX · eMAPT, eEDA, eSOC, eCIR · eCTHP, eCDFP, eAIS · eIAMA · `resources/` — plus one pass
> over the infrastructure (`scripts/`, `.github/`, the root documents). Each pass owned a
> disjoint set of files, so no two writers touched the same file. Every pass was told to run
> the tool before rewriting the command, and to say so when it could not.
>
> **What changed in numbers.** 144 files edited, 3,062 lines added and 836 removed, plus the
> 25 tool catalogues and two scripts that were sitting unpublished in the working tree (see
> *Infrastructure*). The three checks in `.github/workflows/docs-check.yml` are green:
> `check-catalog` and `check-links` were already green, `check-commands` went from **14
> unknown flags or plugins** to **0**.

## How each correction was checked

Four levels, and every file says which one it used in a `> **Verification:**` blockquote
(`CONTRIBUTING.md` documents the convention):

| Level | Meaning | Example |
|---|---|---|
| **A · Executed** | The tool was installed and run; its real output is in the record. | `frida --help` on frida-tools 17.18.0, `vol windows.memmap --help`, `yara` against a `.txt` |
| **B · Parsed** | No runtime, but a validator of the tool itself accepts the artefact. | `node --check` on a JavaScript hook, `sigma check`, `py_compile`, CommonMark fence rules |
| **C · Checked against** | Primary documentation, or the tool's own source code, cited by URL. | Impacket's `hashes.split(':')`, Turbo Intruder's `docs/race-conditions.md`, MASVS control pages |
| **D · Not verified** | Declared as such in the file. Never dressed up as a fact. | SPL/KQL/VQL without a backend, KAPE and Sysmon without Windows, macOS-only tooling |

Tools used at level **A** (versions as installed in WSL Ubuntu 24.04.4 on 2026-09-19):
Volatility 3 Framework 2.28.2 · The Sleuth Kit 4.12.1 · YARA 4.5.0 · OpenSCAP 1.3.9
(+ `autotailor`) · Lynis 3.0.9 · Sleuth Kit `ewfacquire`/`ewfverify` 20140814 · Nmap 7.94 ·
sqlmap · Hydra · tshark/capinfos · sqlite3 3.45.1 · jq · Frida 17.18.0 · Velociraptor 0.77.2 ·
jadx 1.5.1 · sigma-cli 3.1.0 · garak · promptfoo · PyRIT · plaso · Impacket · objection ·
John · Hashcat · GNU Awk 5.2.1 · PowerShell 7.6.6 and Windows PowerShell 5.1 · Node 22.23.2 ·
Python 3.12.3. KAPE, Sysmon, Zeek, Burp, a SIEM, an IdP, macOS and a real evidence device are
**not** available here; each affected note says so.

## 1. Commands and plugins that could not run

The class that started this. Every one of the following was corrected and, where the tool
exists, checked against the installed binary:

| Was | Now | Evidence |
|---|---|---|
| `frida … --no-pause` (4 sites, eMAPT) | no flag, or `--pause` where the text wants the main thread held | `frida --help` (17.18.0) has `--pause  leave main thread paused after spawning program` and nothing else; `-P` is `--parameters`, not a pause flag |
| `vol … windows.memdump --pid X --dump <dir>` (6 sites, eCDFP) | `vol -f img -o DIR windows.memmap --pid X --dump` | `vol windows.memdump` → `invalid choice`; `windows.memmap --help` → `--dump` is a flag and the directory is the global `-o`, which must precede the plugin |
| `vol … windows.yarascan` (3 sites, eCDFP/eCTHP) | `windows.vadyarascan --pid` for process attribution, `yarascan.YaraScan` for a raw scan | `vol windows.yarascan --help` → `invalid choice`; `vol yarascan` → "matches multiple plugins"; `YaraScan` has no `--pid` |
| `vol … windows.hivelist` (eCIR) | `windows.registry.hivelist` | plugin tree and `--help` on 2.28.2 |
| `vol … windows.dumpfiles --pid X --dump` (6 sites, eCDFP) | `vol -f img -o DIR windows.dumpfiles --pid X` | `windows.dumpfiles --help` lists `--pid/--virtaddr/--physaddr/--filter/--ignore-case` and no `--dump` |
| `vol … windows.malfind --pid X --dump <dir>` (eCDFP) | `-o DIR windows.malfind --pid X --dump` | `--dump` is a flag: `Extract injected VADs` |
| `ils -m / image.dd` (7 sites, eCDFP) | `ils -o OFF -m image.dd` | `ils`'s synopsis has `[-m]` with no argument; the wrong form dies with `Invalid magic value (raw_open: image "/" - is a directory)` — the error goes to stderr while the bodyfile silently receives nothing |
| `sigfind -o OFF -l 512 0xAA55 IMG` | `sigfind -b 512 -o 510 -l AA55 IMG` | `sigfind -h`: `-b` is the block size, `-o` is the offset *within* a block, `-l` is a flag. The old form printed `Invaild signature - full bytes only` |
| `nmap -oJ` | removed; the table now lists `-oN/-oX/-oS/-oG/-oA` and says nmap has no JSON output | `nmap --help`: `-oA <basename>: Output in the three major formats at once` |
| `nmap --proxies http://…  # nmap's native SOCKS` | the `proxychains` route, with a note that `--proxies` takes HTTP or SOCKS4 and the scheme picks which | `nmap --help`: `--proxies <url1,[url2],...>: Relay connections through HTTP/SOCKS4 proxies` |
| `SharpHound.exe --ldapuser/--ldappass` | `--ldapusername/--ldappassword` | SharpHound's own README and `scripts/utilities/tool-specs/sharphound.json` |
| `oscap xccdf generate tailoring-file` (4 places + two checklists) | `autotailor --unselect … --output … <ds.xml> <profile>`, plus the fact that the scan must then select the derived `<profile>_customized` | `oscap xccdf generate --help` lists only `report/guide/fix/custom`; `oscap xccdf generate tailoring-file` → `No such module: tailoring-file`; `autotailor --help` |
| `lynis show warnings` / `show suggestions` (5 sites) | `lynis audit system --warnings-only` plus `grep -E ' (Warning\|Suggestion): ' /var/log/lynis.log` and the `warning[]` keys in `/var/log/lynis-report.dat` | `lynis show` prints the argument list, which has no `warnings`; a real run produced `hardening_index=62` and 3 warnings / 45 suggestions |
| `kape --module !EZ1` | `!EZParser`, with `--msource` pointing at what was *collected*, not at the live system | KapeDocs and the repository's own KAPE catalogue |
| `kape.exe --help` | the documented route (`gKAPE`, KapeDocs); the switch is not in KAPE's documentation and no binary was available to test it | KapeDocs; the note says "not verified" |
| `velociraptor --config … client collect …` | `… artifacts collect <Artifact> --client_id C.xxxx` | the real v0.77.2 binary: `-r, --run=RUN` and `--client_id="server"` exist, `client collect` does not |
| `-hashes :LM:NT` (three fields) | `-hashes LM:NT` or `:NT` | Impacket's `hashes.split(':')` raises `ValueError: too many values to unpack (expected 2)` |
| `whoami /groups \| grep …` inside a Windows shell | `findstr /i` (cmd) or `Select-String` (PowerShell) | `grep` does not exist on Windows: exit 255 |
| `-u "name"` and `-t file.E01` in `ewfacquire` (2 sites) | `-u` with no argument, `-t` naming the target **without** extension, `-C/-D/-e` for the case metadata | `ewfacquire -h`: `-u: unattended mode (disables user interaction)`, `-t: specify the target file (without extension)`; the old form produced `dbl.E01.E01` |
| `sqlite … PRAGMA free page counts` | `PRAGMA freelist_count;` | the old form is a syntax error; the correct one returns a number |

## 2. Lab code that could not produce its promised result

- **The regression gate that cannot fail** — `eAIS/labs/guardrail-evaluation-lab.md`. It was
  invoked with `--baseline baseline.json --current baseline.json` while `harness.py` rewrote
  that same file on every run. The harness now writes its own metrics file and the CI example
  compares two distinct files. The lab still says, twelve lines earlier, that a baseline which
  follows the current code always passes: that sentence is now true of the example.
- **A sanitiser that removed the attribute, not the text** — `eAIS/labs/injection-lab.md`. The
  hidden channel survived in the chunk while the drill asked the reader to list the channels
  they had removed.
- **A filter declared fail-closed that opened on a null identity** —
  `eAIS/labs/rag-data-leakage-lab.md`. A null tenant matched every unclassified chunk.
- **An FGSM fragment that could not run with either type** —
  `eAIS/methodology/04-adversarial-attacks.md` mixed numpy and torch APIs.
- **A YARA rule that could never match the file the drill used** —
  `eSOC/tools/detection-rules/yara-rules/yara-example.yar` required `pe.is_pe` while the drill
  ran it against a `.txt`. Executed proof: the old rule produced **no output** for a text file
  with three of its strings and **did** match the same content appended to a PE; the rule added
  for the text drill matches the `.txt`, and a file that merely starts with `MZ` fires only
  that one.
- **A beacon filter that measured the bucketing, not the jitter** — eSOC's SPL binned `_time`
  before computing deltas, so every service looked perfectly regular. Same defect, same family,
  in eCTHP: the rule used absolute σ where the module itself defines the signal as σ/mean.
  Executed with synthetic logs: `jitter < 5` **admitted** noisy traffic (σ 2.3 s) and
  **rejected** a real slow beacon (σ 11.4 s, CV 0.019).
- **"Unique subdomains" that counted queries** — missing `sort -u` before the `awk`. Executed:
  one name repeated 50 times was reported as 50 subdomains.
- **A JavaScript hook that was a syntax error** — `eMAPT/methodology/04-communication-analysis.md`
  had `function (untrustedChain, ...)`. `node --check` on the old fragment: `SyntaxError:
  Unexpected token ')'`; on the replacement: exit 0.
- **A CL.TE payload whose prefix never left the front end** — `Content-Length: 4` over a
  29-byte body. Counted byte by byte, and the confirmation criterion now matches the payload.
- **An ID token authorised without validation** — `eIAMA/labs/iam-scenarios.md` decoded claims
  and trusted them. It now validates signature, `iss`, `aud`, `exp` and `nonce`, which the
  module's own cheatsheet already required. Executed with a local key as the realm's JWKS:
  the old callback accepted a forged signature, a wrong issuer, an expired token and an
  unsigned `alg=none` token; the corrected one rejects all four and accepts the valid one.
- **A harness that reported the inverse of what it measured** — `eAIS/labs/guardrail-evaluation-lab.md`
  assigned `decide()`'s "allowed" flag to `blocked`, so the lab printed the *allow* rate as the
  block rate and scored a control that blocks everything as 0. Found while verifying the two
  fixes above, because nothing else explained the numbers: with all checks enabled the harness
  reported `block_rate 1.0 / fp_rate 1.0`; with `--checks input` it reports `0.5 / 0.0`, and the
  format blocks (35) are now counted apart from the security blocks (15).
- **A Keycloak lab that the audit described too strongly** — the fix pass ran Keycloak 26.1.0
  three times (old variables, new variables, none) and **measured** the opposite of what the
  audit assumed: `KEYCLOAK_ADMIN*` is deprecated but still honoured, with
  `WARN KC-SERVICES0110` and a working admin login; with no variables at all the login returns
  401, so the test discriminates. The lab now uses `KC_BOOTSTRAP_ADMIN_USERNAME` /
  `KC_BOOTSTRAP_ADMIN_PASSWORD` because those are the supported names, and the note says
  "deprecated alias", not "broken".
- **Evidence written to the system under investigation** — eCIR's containment block and
  cheatsheet captured to `C:\` and to relative paths, against the module's own rule, before a
  reimage that would have destroyed the only copy.
- **A scanner that claimed not to follow redirects and did** — `urllib.request.build_opener`
  adds `HTTPRedirectHandler`, so the 301/302 branch was dead. Repaired in the code, and
  re-tested against a local server that redirects; the branch now fires.
- **A manifest that "verified the image" while hashing the source** — eCDFP's
  `sha256sum -c evidence-source.sha256` re-read the device. The flow now writes one manifest
  per artefact, each naming the file it describes.

## 3. Internal contradictions

- **eCDFP** called ShimCache/Amcache "evidence of executed programs" where the same module,
  later, insists "Prefetch proves execution. ShimCache proves presence".
- **eJPT** explained the bind shell backwards, against its own table and its netcat guide.
- **eEDA** listed five CSF functions in a module that documents CSF 2.0 (six), mapped admin MFA
  to CIS Control 5 (no MFA safeguard in it — it is 6.5), and gave three different patch windows
  for the same critical severity.
- **eSOC** put encoded PowerShell in "Medium" on the triage sheet while the body — and its own
  Sigma rule — call it `high`.
- **eCTHP** denied that VQL has `GROUP BY` in one row and used it in the next; used `FullPath`
  where the current artefact selects `OSPath`; and counted five nodes in a four-VM range.
- **eAIS** promised "one schema across the module" while three incompatible schemas coexisted,
  and six files warned that sibling notes "were not written yet" when all 25 existed.
- **eWPTX** inverted its own HPP example against its cheatsheet, and its SAML comment-injection
  and signature-wrapping examples described the opposite of what the attack does.

## 4. Frameworks, standards and identifiers

CSF 2.0's sixth function (`Govern`) is now present everywhere it is listed · admin MFA cites
CIS **6.5** · PCI DSS is versioned (v4.0.1) with Requirement 7's current wording · eADA's
"ISO 27002 Annex A" became ISO/IEC 27001:2022 Annex A · eMAPT enumerates the **eight** MASVS
categories and quotes real control IDs · eAIS names the ATLAS technique that exists
(*LLM Prompt Injection*, `AML.T0051`) instead of one that does not, and versions its OWASP LLM
Top 10 citations instead of mixing the 2023 and 2025 numbering · eIAMA no longer claims AAL2
requires verifier-impersonation resistance, separates SP 800-207's tenets from industry slogans,
and cites RFC 9700/OAuth 2.1 for the deprecations it states · eCIR stops calling Recovery "a
distinct NIST phase" (Rev. 2 groups containment, eradication and recovery in one) and cites the
withdrawn Rev. 2 lifecycle as such · eCDFP corrects the title of NIST SP 800-101 and uses
`--symbol-dirs` · eSOC fixes the Sigma rule whose `logsource` could not see what its title
claimed and the T1218 name.

## 5. External facts and links

- **Twenty-five dead external links replaced** with the live page for the same resource
  (verified by HTTP status before editing): Microsoft Learn's auditing and `wevtutil`
  references, the Sleuth Kit docs, OWASP's cheat-sheet series and community pages, NIST
  SP 800-53r5 / SP 800-92 / SP 800-37r2 / AI 600-1 / AI 100-2 E2025, MISP's warning lists,
  Wazuh's alert management, CISA's incident-response playbooks, EvtxECmd's new repository,
  KAPE's definitions repository, MASTG (18 occurrences across eMAPT), the ML Top 10 and the
  SAML V2.0 core specification.
- **A date that was no longer the latest**: `README.md` said eCTHP's last public update was
  24 July 2025; INE announced a further update on 20 November 2025 — the very link the
  repository already cited, but described as background to the July event. eSOC's launch
  announcement (3 March 2026) filled a blank cell.
- **A domain that is not INE's**: eCPPT's checklist pointed readers at `inecp.com`.

## 6. Repository infrastructure and governance

- **The checker could not see four of the six defect classes its own header named.** It now
  reads fenced blocks in `bat`, `cmd`, `ps` as well as shell languages, inline code spans
  that look like a command (that is how the fourth `--no-pause`, inside prose, was found),
  and **tables of flags** when the table's context names exactly one catalogued tool — the
  shape that let `nmap -oJ` sit in a table for two audits. It accepts universal flags such as
  `--help`; knows which flags take a value, so `velociraptor -r Windows.System.Pslist` is not
  reported as an unknown plugin; skips audit records, which quote broken commands on purpose;
  and provides an explicit escape (`<!-- check-commands: ignore -->`) for a guide line that
  quotes a wrong invocation in order to warn about it. Both new behaviours were tested against
  a deliberately broken copy: an invented flag in a table makes the check exit 1, and the same
  row with the escape marker makes it exit 0.
  One blind spot remains and is stated in its header: `-oJ` is read as `-o` with the value `J`
  attached, because the same prefix rule is what accepts `-T4` and `-p80`. Real nmap does
  something worse than error — `nmap -oJ salida.json 127.0.0.1` writes a file called `J` and
  treats `salida.json` as a target — which is exactly why that row mattered.
- **A second check covers what no flag catalogue can.** `scripts/utilities/check-code.mjs`
  fails on a fenced block that is never closed (one shipped here, swallowing the last third of
  a file) and on any `python` or `js` block, or `.py`/`.mjs` file, that does not parse. It
  executes nothing: Python is parsed with `ast.parse`, JavaScript with `vm.Script`, and Python
  blocks are reported as skipped rather than passing on a machine without Python 3. Tested
  against a copy with four deliberate defects — an unclosed fence, a Python syntax error, a
  JavaScript `function (x, ...)` and a broken standalone script — it found all four and exited
  1.
- **The weekly external sweep could never pass**: three RFC 2606 placeholders (`*.example`)
  and the deliberately-dead `…/security/certifications` index were permanent failures. Both are
  now recognised: reserved names never fail, and a short documented list of URLs the repository
  cites *because* they are dead is skipped. The sweep now reports only real rot.
- **`tool-installer.py` ran the package name as the installer** (`apt` vs `apt-get`) and then
  printed `installed`. It now resolves the package per manager, refuses unknown tools, verifies
  the manager afterwards, and treats `--dry-run` as the default it always claimed to be.
- **`env-setup.sh` probed for a command that cannot exist** (`python3-pip`), so it reinstalled
  every time and aborted on managers that do not have that package; it also reported success
  with an unknown manager. It now separates commands from packages, stops on an unknown
  manager, and checks that the binary appeared.
- **`report-generator.py` showed "100%" for files with no checklist** and grouped every file
  into one module. It now reports `n/a`, groups by area/module, and states how many notes were
  excluded.
- **Twenty-five tool catalogues and two rewritten scripts were unpublished** in the working
  tree: that is why the published CI was green while the local checker reported 14 defects.
  They are published with this pass, so the check runs with the coverage it was written to have.
- **The root documents were out of step with the code**: `README.md` and `CONTRIBUTING.md` said
  "two checks" while CI runs three and now describe all three; `README.es.md` is labelled as an
  orientation page rather than a translation, and states where the figures live; `.gitignore`
  is commented in English as the project promises, with `node_modules/` added; the previous
  audit's summary table — five placeholder rows and a total that contradicted its own thirteen
  rows — was recomputed from the findings (112 entries: 21 Blocking, 57 Important, 33 Minor,
  1 Preference; 4 modules "not apt", 9 "with reservations").
- **How to say you checked something** is now a written convention, with the marker for
  counter-examples, and the checker measures adoption: it went from **0/162 files** to
  **78/165 (47%)** during this pass.

## 7. What this pass did not verify

Stated once here, and again in each affected file:

- **No SIEM, no lab range, no forensic image, no domain, no mobile device, no macOS host, no
  physical evidence device.** SPL, KQL, EQL, VQL, osquery and Sysmon examples remain
  syntax references; the ES|QL blocks added to eSOC were written by construction and by
  contrast with the module's own reference, not run.
- **KAPE, Zeek and Burp** are not installed: KAPE's module names come from its documentation
  and from this repository's catalogue; Zeek examples were translated to `awk` over TSV data to
  make them runnable; Turbo Intruder's gate was confirmed against its own documentation.
- **plaso** was installed after the eCDFP pass had finished, so its invocations remain
  references. **`vol.py` does not exist** in a pip install (the console script is `vol`);
  the guides use the source-checkout form, which is correct for a clone, and the affected note
  now says which is which.
- **`sigma convert`** could not run: sigma-cli 3.1.0 has no backend plugins installed here.
- **`garak`/`PyRIT` flag and import names** are version-sensitive and the eAIS notes say so; the
  installed versions were used where a run was possible.
- **The 21 Blocking findings of the previous audit are not all closed by name.** This pass
  closed the ones listed above; the remainder are the ones in this section and the items below.

## 8. Still open

1. **Field names in vendor queries.** `SubStatus`, `New_Process_Name` and the Splunk Add-on's
   field naming could not be checked against a real mapping; eSOC omits the one it could not
   confirm rather than guessing.
2. **Sysmon event introduction versions (23–29).** Sysinternals no longer publishes the
   per-version changelog; the note now points at `sysmon64.exe -s` instead of asserting a range.
3. **SP 800-61 Rev. 2's exact phase wording.** The PDF text could not be extracted here; the
   files cite Rev. 3's real title and mark the Rev. 2 lifecycle as the withdrawn vocabulary it
   is.
4. **MASVS/MSTG naming in `resources/`** and the eIAMA "Associate" claim: the first is fixed,
   the second is marked as unsupported by any source found.
5. **`eNDP` (Network Defense Professional)** appears in old eLearnSecurity material with no
   product page on `ine.com`. Whether it is still examinable could not be established; the
   repository neither lists it as current nor as retired.

## 9. Re-running the checks

```console
$ node scripts/utilities/check-catalog.mjs .
$ node scripts/utilities/check-links.mjs .
$ node scripts/utilities/check-commands.mjs .
$ node scripts/utilities/check-links.mjs . --external   # network; also the weekly job
```

To reproduce the executions quoted above, the tools live in the WSL Ubuntu 24.04 install used
for this pass (Volatility 3, Sleuth Kit, YARA, OpenSCAP, Lynis, tshark, sqlmap, hydra,
Velociraptor, frida-tools, sigma-cli, plaso, garak, promptfoo and friends). Nothing in the
repository requires them: they were the reviewer's instruments, not dependencies.
